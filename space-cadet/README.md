# Install and play

```sh
python3 -m venv venv/ # Create a virtual environment for the repo.
source venv/bin/activate # Use Activate.ps1 if you're on windows
pip3 install -r requirements.txt # download dependencies for game
python3 main.py
```
